@MixinConfigOption(description = "Store the server and client profiler in static fields to avoid a map lookup. Skip looking up the profiler when no profilers are active.")
package net.caffeinemc.mods.lithium.mixin.profiler;

import net.caffeinemc.gradle.MixinConfigOption;